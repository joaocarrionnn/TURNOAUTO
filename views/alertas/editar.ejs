<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <%- include('../partials/head', {title: 'Editar Alerta'}); %>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
  <%- include('../partials/header', {user: user}); %>
  
  <main class="container mx-auto px-4 py-6">
    <div class="max-w-2xl mx-auto">
      <h1 class="text-2xl font-bold text-gray-800 mb-6">Editar Alerta</h1>
      
      <% if (typeof error !== 'undefined' && error) { %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4">
          <%= error %>
        </div>
      <% } %>
      
      <form action="/alertas/editar/<%= alerta.id %>" method="POST" class="bg-white shadow-md rounded px-8 pt-6 pb-8 mb-4">
        <div class="mb-4">
          <label class="block text-gray-700 text-sm font-bold mb-2" for="titulo">
            Título
          </label>
          <input class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
            id="titulo" name="titulo" type="text" placeholder="Título do alerta" value="<%= alerta.titulo %>" required>
        </div>
        
        <div class="mb-4">
          <label class="block text-gray-700 text-sm font-bold mb-2" for="descricao">
            Descrição
          </label>
          <textarea class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
            id="descricao" name="descricao" rows="4" placeholder="Descreva o alerta em detalhes" required><%= alerta.descricao %></textarea>
        </div>
        
        <div class="mb-4">
          <label class="block text-gray-700 text-sm font-bold mb-2" for="tipo">
            Tipo
          </label>
          <select class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
            id="tipo" name="tipo" required>
            <option value="Segurança" <%= alerta.tipo === 'Segurança' ? 'selected' : '' %>>Segurança</option>
            <option value="Manutenção" <%= alerta.tipo === 'Manutenção' ? 'selected' : '' %>>Manutenção</option>
            <option value="Operacional" <%= alerta.tipo === 'Operacional' ? 'selected' : '' %>>Operacional</option>
            <option value="Outro" <%= alerta.tipo === 'Outro' ? 'selected' : '' %>>Outro</option>
          </select>
        </div>
        
        <div class="mb-4">
          <label class="block text-gray-700 text-sm font-bold mb-2" for="severidade">
            Severidade
          </label>
          <select class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
            id="severidade" name="severidade" required>
            <option value="crítica" <%= alerta.severidade === 'crítica' ? 'selected' : '' %>>Crítica</option>
            <option value="alta" <%= alerta.severidade === 'alta' ? 'selected' : '' %>>Alta</option>
            <option value="média" <%= alerta.severidade === 'média' ? 'selected' : '' %>>Média</option>
            <option value="baixa" <%= alerta.severidade === 'baixa' ? 'selected' : '' %>>Baixa</option>
          </select>
        </div>
        
        <div class="mb-4">
          <label class="block text-gray-700 text-sm font-bold mb-2" for="status">
            Status
          </label>
          <select class="shadow appearance-none border rounded w-full py-2 px-3 text-gray-700 leading-tight focus:outline-none focus:shadow-outline" 
            id="status" name="status" required>
            <option value="ativo" <%= alerta.status === 'ativo' ? 'selected' : '' %>>Ativo</option>
            <option value="resolvido" <%= alerta.status === 'resolvido' ? 'selected' : '' %>>Resolvido</option>
            <option value="cancelado" <%= alerta.status === 'cancelado' ? 'selected' : '' %>>Cancelado</option>
          </select>
        </div>
        
        <div class="flex items-center justify-between">
          <a href="/alertas" class="inline-block align-baseline font-bold text-sm text-gray-600 hover:text-gray-800">
            Cancelar
          </a>
          <button class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded focus:outline-none focus:shadow-outline" type="submit">
            Atualizar Alerta
          </button>
        </div>
      </form>
    </div>
  </main>
  
  <%- include('../partials/footer'); %>
</body>
</html>